accelerate
numpy
pandas
scikit-learn
sentencepiece
torch
tqdm
transformers
# Behavior experiment generation (GPU host only); also available via the `behavior` extra.
vllm
# LLM-judge labeling via OpenRouter; also available via the `judge` extra.
openai
# ToxiGen data prep (`latent-align prepare-toxigen`); also available via the `toxigen` extra.
datasets
# Analysis & reporting (runs/analysis.py + notebooks/analysis_pa_ccs_vs_judges.ipynb).
matplotlib
scipy
